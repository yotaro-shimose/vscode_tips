def some_computation(
    a: int,
    lot: float,
    of: str,
    arguments: list[str],
):
    """do some stupid computations.

    Args:
        a (int): it is a
        lot (float): it is b
        of (str): it is c
        arguments (list[str]): it is d
    """
    ...
